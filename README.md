# arnor.cc

A small static contact site for `arnor.cc`.

## Local preview

Open `index.html` in a browser, or serve the folder with any static file server.

## Deploy

This site is plain HTML and CSS. It can be hosted on any static host by publishing the repository root.
